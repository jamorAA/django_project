meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"
build:
  requirementsPath: requirements.txt
run:
  command: gunicorn --bind 0.0.0.0:80 app.wsgi:application
  persistenceMount: /data
  containerPort: "80"
