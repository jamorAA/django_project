meta:
  environment: python
  toolchain:
    name: pip
    version: "3.13"
build:
  requirementsPath: requirements.txt
run:
  command: python manage.py makemigrations && python manage.py migrate && python manage.py loaddata fixtures/goods/categories.json && python manage.py loaddata fixtures/goods/products.json && gunicorn app.wsgi:application --bind 0.0.0.0:80
  persistenceMount: /data
  containerPort: "80"
